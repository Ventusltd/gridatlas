# Text is LF, everywhere, in the working copy as well as in the blob.
#
# WHY THIS FILE EXISTS
#
# Every digest in this repository is taken over git blob content, which is LF,
# and GitHub Pages serves those same bytes. On a Windows checkout with
# core.autocrlf=true git writes CRLF into the working copy instead, so the file
# on disk is not the file that ships — and anything that reads the disk to
# answer a question about what ships gets a different answer depending on whose
# machine it runs on.
#
# That defect appeared FOUR times in one evening, in four separate places:
#
#   the pipelinenews release verifier   hashed the working copy
#   tools/scope/verify-compose.mjs      failed on a tree nobody had touched
#   tools/scope/advance.mjs             RECORDED digests from the working copy,
#                                       so it would have attested bytes GitHub
#                                       Pages never serves
#   the cartridge proof                 compared the shell adapter raw, and so
#                                       passed on Windows and failed on the
#                                       runner — where it was right to fail
#
# Each was fixed by normalising at the point of comparison. This fixes the
# cause: with `* text=auto eol=lf` the working copy is LF too, so there is no
# second version of a file to disagree with, and a checkout on Windows, on a
# runner, and in a container all hold identical bytes.
#
# `eol=lf` is deliberate rather than bare `text=auto`. Bare auto normalises the
# blob and still hands Windows CRLF, which is exactly the situation that caused
# all four.

* text=auto eol=lf

# Binaries must never be touched. Line-ending translation inside a PNG or a
# Parquet file corrupts it silently, and the corruption survives a commit.
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.webp   binary
*.ico    binary
*.pdf    binary
*.zip    binary
*.gz     binary
*.zst    binary
*.parquet binary
*.woff   binary
*.woff2  binary
*.ttf    binary
*.otf    binary
*.mbtiles binary
*.pbf    binary

# Shell scripts are executed by Linux runners. A CR at the end of a shebang
# line makes the kernel look for an interpreter whose name ends in a carriage
# return, and the error it gives — "bad interpreter" — names a file that
# appears to exist.
*.sh     text eol=lf
*.bash   text eol=lf

# So are the things CI runs directly.
*.mjs    text eol=lf
*.js     text eol=lf
*.py     text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.json   text eol=lf
*.geojson text eol=lf
*.md     text eol=lf
*.html   text eol=lf
*.css    text eol=lf
*.txt    text eol=lf

# This composed cartridge carries the immutable V8 engine byte-for-byte. The
# source engine contains historical end-of-line spaces, so treating those
# carried bytes as newly authored whitespace would require changing the slot
# contract. Keep the bytes/digests reproducible and exempt this exact artefact.
atlas/cartridges/202609040021-substation-intelligence-v9-63.js -whitespace
# The receiver successor deliberately preserves every untouched V8 byte,
# including those historical spaces. Exempt only the exact carried input and
# the two cartridges assembled from it; authored modules remain checked.
atlas/parts/202609040045-ventus-corev8engine-deep-link-receiver.js -whitespace
atlas/cartridges/202609040046-substation-intelligence-v9-63.js -whitespace
atlas/cartridges/202609040047-substation-intelligence-v9-63.js -whitespace
atlas/cartridges/202609040058-substation-intelligence-v9-63.js -whitespace
atlas/cartridges/202609040134-substation-intelligence-v9-63.js -whitespace
# This successor also preserves the untouched V8 engine bytes, including its
# historical trailing spaces. Limit the exemption to the new immutable input
# and the one generated cartridge which contains it.
atlas/parts/202609040212-ventus-corev8engine-layer-availability.js -whitespace
atlas/cartridges/202609040219-substation-intelligence-v9-63.js -whitespace
# This exact successor preserves the same untouched V8 bytes while removing
# only the obsolete cross-domain deep-link fetch block.
atlas/parts/202609040229-ventus-corev8engine-exact-repd-delegation.js -whitespace
# The composed SLD successor retains its body's terminal blank byte exactly;
# scope the diff exemption to this source and its immutable composed output.
atlas/parts/202609040229-sld-sandbox-arrival-identity.js -whitespace
atlas/cartridges/202609040337-sld-sandbox-v9-8.js -whitespace
atlas/cartridges/202609040337-substation-intelligence-v9-63.js -whitespace
# v9.107 moves stylesheet bytes only; its recomposed network cartridge still
# carries the same 58 historical V8 trailing-space lines byte-for-byte.
atlas/cartridges/202609040403-substation-intelligence-v9-63.js -whitespace
# v9.108 restamps this cartridge for the menu-bar attribution-clearance fix
# only; the carried V8 engine input and its 58 trailing-space lines are
# byte-identical to the exemption above.
atlas/cartridges/202609041221-substation-intelligence-v9-63.js -whitespace
# v9.109 restamps this cartridge to carry the version-ledger's own new
# entry forward (the technology-bucket fix landed in sld-sandbox); the
# carried V8 engine input and its 58 trailing-space lines are unchanged.
atlas/cartridges/202609041244-substation-intelligence-v9-63.js -whitespace
# v9.110 restamps this cartridge for the menu-bar/masthead/SCADA-panel
# restoration only (atlas/modules/202609031958-menu-bar.js); the carried
# V8 engine input and its 58 trailing-space lines are unchanged.
atlas/cartridges/202609041250-substation-intelligence-v9-63.js -whitespace
# v9.112 restamps this cartridge so the GRID and SUBS chips stay on the map
# at phone widths instead of inside the Grid menu
# (atlas/modules/202609031958-menu-bar.js); the carried V8 engine input and
# its 58 trailing-space lines are unchanged.
atlas/cartridges/202609041945-substation-intelligence-v9-63.js -whitespace
atlas/cartridges/202609041956-substation-intelligence-v9-63.js -whitespace
atlas/cartridges/202609041957-substation-intelligence-v9-63.js -whitespace
