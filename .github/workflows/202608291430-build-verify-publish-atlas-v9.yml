name: 202608291430 Legacy staged publication archive
run-name: Read archived 202608291430 publication status

on:
  workflow_dispatch:

permissions: {}

jobs:
  archived:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Record retirement boundary
        run: |
          {
            echo '## Archived 202608291430 workflow'
            echo
            echo '- Automatic state-pointer and release-path triggers were retired on 29 August 2026.'
            echo '- The immutable 202608291430 release remains preserved.'
            echo '- The active successor is the map-ready comparator-gated 202608292126 release programme.'
            echo '- No repository mutation is performed here.'
          } >> "$GITHUB_STEP_SUMMARY"
