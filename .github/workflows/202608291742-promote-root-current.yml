name: 202608291742 Legacy root promotion archive
run-name: Read archived root promotion status

on:
  workflow_dispatch:

permissions: {}

jobs:
  archived:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Record retirement boundary
        run: |
          {
            echo '## Archived root promotion workflow'
            echo
            echo '- Automatic workflow-run promotion was retired on 29 August 2026.'
            echo '- It could otherwise restore the obsolete 202608291430 pointer.'
            echo '- The successor workflow performs comparator-gated promotion of 202608292126-atlas-v9.'
            echo '- No repository mutation is performed here.'
          } >> "$GITHUB_STEP_SUMMARY"
