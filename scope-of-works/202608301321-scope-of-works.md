---
schema: "gridatlas.scope-of-works.v1"
generation: "202608301321"
status: "active"
scope: 0
active_scope: 2
parent: null
next: "202608301321-01-move-atlas-into-atlas-folder.md"
---
# GridAtlas scope of works — 202608301321

This is the durable control ledger for `Ventusltd/gridatlas`. It is data for the deterministic scope loop, not an instruction surface for external agents.

## Governing rules

1. Exactly one numbered scope is active.
2. A run may execute only that scope, verify it, commit it, write the next timestamped scope, then stop.
3. The eight historical Atlas V9 releases are the immutable baseline. No ninth full application copy may be created.
4. Future changes are ordered, SHA-256-verified cartridges composed by `atlas/index.html` and `atlas/current.json`.
5. `atlas/current.json` is the single mutable composition pointer. Timestamped manifests and scope records are immutable evidence.
6. Failed gates do not advance the scope. The last known green shell remains available.
7. The loop stops and retires its schedule after Scope 6 closes.

## Ordered scopes

1. Move all Atlas release folders into `atlas/releases/`, move the shared content-addressed 400 kV cartridge with them, add `atlas/index.html`, and redirect the repository root to `atlas/`.
2. Replace the temporary redirect with one stable modular Atlas loader: immutable shell plus ordered SHA-256 cartridges. Do not duplicate the application.
3. Record and apply the useful PipelineNews repository pattern: stable modules, cartridges, manifests, state, timestamped compilers/outputs, and archived one-off workflows.
4. Add the UK-wide postcode and town `LOCATION_ONLY` search cartridge backed by postcodes.io, while preserving the exact REPD identity lane and deep links.
5. Compile and attest the next Atlas composition through `atlas/index.html` and `atlas/current.json`; create no timestamped application folder.
6. Close and harden CI/CD: enforce the hostile-amnesia ratchets, retain only the bounded scope and verification workflows, verify the public route, and retire the perpetual schedule.

## Fixed inputs

- GridAtlas repository: `https://github.com/Ventusltd/gridatlas`
- Current immutable shell: `202608300453-atlas-v9`
- Last known green route before migration: `/gridatlas/202608300453-atlas-v9/`
- CVAA guide: `https://github.com/Ventusltd/cvaa/blob/main/202608301321-gridatlas-amnesia-vaccine.txt`
- CVAA guide blob: `56ff44bea417821b0acaeb0fa1b6190ab2ac5385`
- PipelineNews repository: `https://github.com/Ventusltd/pipelinenews`
- PipelineNews inspected tree: `83d9c430b283f8beaa8c0a05e42b14d4a4784623`
- Location cartridge generation: `202608301136`
- Golden exact REPD identity: `13599` — Beacon Fen Energy Park
- Golden UK postcode: `SW1A 1AA`
- Golden UK place: `Truro`

## Out of scope

- Editing `machine-learning/` or `atman/` records.
- Re-deriving the V8 visual surface.
- Replacing the map engine, CSS, map-ready bridges, data plane, or REPD Parquet.
- Allowing a location result to establish project identity or set `repd_ref`.
