# 202608310015 attached sample study findings

This is a compact, hash-linked study digest of the nine attached `_build-plan` samples. The complete local folder is stated to exceed 43,000 words and remains authoritative once committed. Exact bytes and SHA-256 values for every attached sample are in `202608310015-attached-samples-lock.json`.

## Governing product frame

The system is a timing engine for grid-connection sales. `globalgrid2050` is the origin hub being split into specialised application and data repositories. The highest risk during that split is a broken seam: a consumer continuing to point at a producer path that was reorganised. The intelligence product is the ability to show which projects are in the funding/procurement window now, backed by register evidence and explicit abstention.

## Confirmed current defect: N1

PipelineNews and Companies still emit `https://ventusltd.github.io/gridatlas/202608300453-atlas-v9/`, while GridAtlas moved immutable releases under `atlas/releases/` and the stable composed product is `https://ventusltd.github.io/gridatlas/atlas/`. The stale producer route is a 404. The recommended repair is the stable composed route, not the immutable shell route, because the latter is uncartridged and silently downgrades receiver behaviour.

Required N1 proof:

- stable `?repd_ref=13599` resolves Beacon Fen;
- root redirect preserves the same query;
- stable `?repd_ref=17494` resolves East Pye;
- stale root release path is confirmed as 404;
- proof is real Chromium, no route interception, no synthetic receiver;
- producer pointers move only after proof; overnight work creates candidates only.

## N2: exact reference index

A deep link currently boots DuckDB-WASM and reads the REPD Parquet before it can resolve. The runtime is 35.7 MB, equal to 14.28 seconds at the declared 20 Mbit reference before data is read. Build a minimal static index for all 11,069 exact REPD references with row form:

`[name, technology, status, capacity_mw, longitude, latitude]`

The index must be hash-pinned to `data/repd_projects_202608290716.parquet`, whose expected SHA-256 is `174040c37f3d63742d6fdd7af722a8cfdf3fb53de3ff85ff1142d22fdac4866b`. It is data, not a full app. Cartridge installation remains blocked until the reviewed anchored patch and spec arrive under `DRAFT-CARTRIDGES/`.

## Data delivery law

- DuckDB is the explicit search plane only.
- Light drawing layers use hash-pinned map-ready GeoJSON.
- Heavy national line layers use PMTiles with zoom floors and viewport-bounded transfer.
- Fidelity and delivery are separate verdicts and cannot be traded against each other.
- Current layer gate: 15 seconds, 400 MB heap, at least one feature.
- Heavy layers identified: primary roads, trunk roads, mainline rail and motorways.
- Static exact-ref lookup must work on constrained devices without starting DuckDB.

## Cartridge law

The immutable shell has four script slots: the bridge is taken, search is taken, the engine is forbidden, and the pre-snapped config adapter is the one remaining slot. A future window-intelligence cartridge must be composite and reproduce `snap: false` for 400/275/220/132/66 kV before adding any layer. It must insert the Funding Window group first so it is visible on iPad portrait without scrolling. Failure of its data or manifest must add zero window layers while leaving the core map unchanged.

## Cartridge catalogue

- C1 exact-ref-index — static lookup, priority 1.
- C2 window-intelligence — centrepiece, map-ready GeoJSON, priority 2.
- C3 official REPD v9 map layer.
- C4 governed PipelineNews project spine.
- C5 Companies candidate search Parquet; never draw it.
- C6 precomputed grid-connection screening properties.
- C7 distress overlay, highest paint/state precedence.
- C8 contractor exposure, confirmed organisations only.
- C9 highways PMTiles.
- C10 data centres, source-element-only identity.
- C11 interconnectors, currently blocked by no landed data.
- C12 GB electricity HUD, must carry as-of/stale state.
- C13 heavy-industry offtakers, currently quarantined reproducibility issues.
- C14 deep-link-out, blocked until PipelineNews has an inbound exact-ref receiver.

## N3 governance

CVAA has 24 vaccines and a reusable pinned workflow, but no consumer repository directly calls it. GridAtlas re-implements some antibodies inline. Adoption must be pinned to reviewed commit `d2ebc01f6eab41f2a84b0c53c4cfae0d2625ec5e`, measure actual findings first, then create a short-dated ratchet baseline. Do not install predicted maxima as if measured. Adding an inoculate workflow requires updating GridAtlas `ACTIVE_WORKFLOWS` in the same commit.

## N4 design-freeze calibration

This is dependency-free arithmetic over the frozen 7,680-project PipelineNews spine. For projects with both planning permission and construction dates, compute `under_construction - planning_permission_granted`, then median by technology and capacity band. A cell with fewer than 30 samples is NULL, not guessed. The estimate remains derived and must publish sample size.

## N5 source cards

The shipped product calls postcodes.io and Nominatim without source cards. REPD's card is still unstudied. Needed cards also include PlanIt, planning.data.gov.uk, The Gazette, LCCC, Companies House bulk and Companies House REST. A draft card is not approval: licence, attribution, rate limits, allowed use and last-checked date must be reviewed before a source becomes declared truth.

## Companies and project vehicles

The existing Companies product is a compact candidate generator, not an ownership register. Its relationship tables are three columns: company number, REPD reference and evidence type; all roles remain unknown/abstain. A separate project-vehicle projection may be built only for defensibly bound vehicles, keeping balance-sheet data and event data physically separate. The load-bearing gate is `unbound_vehicle_rows == 0`. Person identities, directors, individual PSC identities, dates of birth and addresses remain excluded; count-only changes are permitted as corporate events.

## Window intelligence build order

The attached plan describes N0–N14. The operational order is:

1. N1 repair the broken cross-repository seam.
2. N0 unblock or deliberately supersede the six-scope ledger before authored campaign commits.
3. N2 exact-ref-index.
4. N3 measured CVAA adoption.
5. N4 calibration and N5 source cards in parallel.
6. N6 register adapter, N7 project-vehicle events, N8 state machine/ranker.
7. N9 visible window-intelligence cartridge after two silent generations.
8. N10 official REPD map data, N11 highways PMTiles, N12 smaller search/exposure cartridges.
9. N13 close the round trip only after PipelineNews has an inbound exact-ref receiver.
10. N14 wire CVAA to the federation dependency dashboard once its location is established.

## Required missing artefacts

The sample set refers to, but does not include:

- `window-intelligence.md`;
- `questions.md`;
- `DRAFT-CARTRIDGES/exact-ref-index.spec.md`;
- `DRAFT-CARTRIDGES/exact-ref-index.js.txt`;
- `DRAFT-CARTRIDGES/window-intelligence.spec.md`;
- `DRAFT-CARTRIDGES/window-intelligence.js.txt`.

Later stages must block rather than infer these files.
